# Local development (the default): no model API key is required.
LLM_PROVIDER=mock
HOST=127.0.0.1
PORT=3000

# Optional path reserved for persistence in a future task.
# DATABASE_PATH=./data/triage.sqlite

# OpenAI configuration names. The OpenAI adapter is planned for a later task.
# LLM_PROVIDER=openai
# OPENAI_API_KEY=
# OPENAI_MODEL=
