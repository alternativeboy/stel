# Local development (the default): no model API key is required.
LLM_PROVIDER=mock
HOST=127.0.0.1
PORT=3000

# Optional local persistence path.
# DATABASE_PATH=./data/triage.sqlite

# Opt-in OpenAI Responses API settings. Live calls may incur external cost.
# LLM_PROVIDER=openai
# OPENAI_API_KEY=
# OPENAI_MODEL=
# OPENAI_TIMEOUT_MS=30000
# OPENAI_MAX_RETRIES=2
